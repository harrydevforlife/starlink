[project]
name = "pydb"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.3.3",
    "pyarrow>=22.0.0",
    "pytest>=8.4.2",
    "duckdb>=1.0.0",
    "tabulate>=0.9.0",
]

[dependency-groups]
dev = [
    "ruff>=0.14.5",
]
